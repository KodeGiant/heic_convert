---
phase: 01-working-app
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - INPUT-01
  - INPUT-02
  - CONV-02

must_haves:
  truths:
    - "The page loads without errors in a browser"
    - "A visible drop zone accepts dragged files (visual feedback on hover)"
    - "A file picker button is present and labeled"
    - "A quality slider (1-100) is visible with its current value displayed"
    - "A convert button is present"
    - "A results area exists to display converted files"
  artifacts:
    - path: "index.html"
      provides: "Complete HTML structure with embedded CSS and CDN script tags"
      contains: "drop-zone, file-input, quality-slider, convert-btn, results-area"
  key_links:
    - from: "index.html drop-zone"
      to: "app.js dragover/drop handlers"
      via: "id='drop-zone' element"
    - from: "index.html file-input"
      to: "app.js change handler"
      via: "id='file-input' element"
    - from: "index.html quality-slider"
      to: "app.js conversion call"
      via: "id='quality-slider' value (1-100)"
    - from: "index.html results-area"
      to: "app.js result injection"
      via: "id='results'"
---

<objective>
Build the complete HTML structure and CSS styling for the HEIC-to-JPEG converter.

Purpose: Establishes the visual foundation and DOM contract that the JavaScript layer (Plan 02) will bind behavior to. All element IDs defined here are the interface contract between HTML and JS.
Output: index.html — a fully styled, static page ready for JavaScript wiring.
</objective>

<execution_context>
@/home/vs/.claude/get-shit-done/workflows/execute-plan.md
@/home/vs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create index.html with full structure and CDN dependencies</name>
  <files>index.html</files>
  <action>
Create index.html from scratch. Include in the `<head>`:
- `<meta charset="UTF-8">` and `<meta name="viewport" content="width=device-width, initial-scale=1.0">`
- `<title>HEIC to JPEG Converter</title>`
- CDN script for heic2any: `<script src="https://cdn.jsdelivr.net/npm/heic2any@0.0.4/dist/heic2any.min.js"></script>`
- CDN script for JSZip: `<script src="https://cdn.jsdelivr.net/npm/jszip@3.10.1/dist/jszip.min.js"></script>`
- `<script src="app.js" defer></script>`

Body structure (use these exact IDs — they are the contract for app.js):

```
<body>
  <div class="container">
    <h1>HEIC to JPEG</h1>
    <p class="subtitle">Convert Apple photos in your browser — nothing leaves your device</p>

    <!-- Drop zone (INPUT-01 target) -->
    <div id="drop-zone" class="drop-zone">
      <p>Drop HEIC files here</p>
      <p>or</p>
      <label for="file-input" class="file-label">Choose files</label>
      <input type="file" id="file-input" accept=".heic,.HEIC,image/heic,image/heif" multiple hidden>
    </div>

    <!-- Quality control (CONV-02) -->
    <div class="quality-control">
      <label for="quality-slider">JPEG Quality: <span id="quality-value">85</span>%</label>
      <input type="range" id="quality-slider" min="1" max="100" value="85">
    </div>

    <!-- Convert action -->
    <button id="convert-btn" disabled>Convert</button>

    <!-- Status messages -->
    <p id="status" class="status" hidden></p>

    <!-- Results area (DL-01, DL-02 targets) -->
    <div id="results" hidden>
      <div class="results-header">
        <h2>Converted files</h2>
        <button id="download-all-btn">Download all as ZIP</button>
      </div>
      <ul id="results-list"></ul>
    </div>
  </div>
</body>
```
  </action>
  <verify>
    <automated>test -f /home/vs/src/heic_convert/index.html && grep -q 'id="drop-zone"' /home/vs/src/heic_convert/index.html && grep -q 'id="quality-slider"' /home/vs/src/heic_convert/index.html && grep -q 'heic2any' /home/vs/src/heic_convert/index.html && grep -q 'jszip' /home/vs/src/heic_convert/index.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>index.html exists with drop-zone, file-input, quality-slider, convert-btn, results, and both CDN script tags present</done>
</task>

<task type="auto">
  <name>Task 2: Add embedded CSS styling</name>
  <files>index.html</files>
  <action>
Add a `<style>` block inside `<head>` of index.html. Style rules:

- `body`: `font-family: system-ui, sans-serif; background: #f5f5f5; margin: 0; padding: 2rem; color: #222;`
- `.container`: `max-width: 640px; margin: 0 auto; background: #fff; padding: 2rem; border-radius: 12px; box-shadow: 0 2px 12px rgba(0,0,0,0.08);`
- `h1`: `margin: 0 0 0.25rem; font-size: 1.8rem;`
- `.subtitle`: `color: #666; margin: 0 0 1.5rem;`
- `.drop-zone`: `border: 2px dashed #ccc; border-radius: 8px; padding: 2.5rem 1rem; text-align: center; cursor: pointer; transition: border-color 0.2s, background 0.2s;`
- `.drop-zone.dragover`: `border-color: #0066ff; background: #f0f5ff;` (applied by JS on dragover)
- `.drop-zone p`: `margin: 0.25rem 0; color: #555;`
- `.file-label`: `display: inline-block; margin-top: 0.5rem; padding: 0.5rem 1.25rem; background: #0066ff; color: #fff; border-radius: 6px; cursor: pointer; font-size: 0.9rem;`
- `.file-label:hover`: `background: #0052cc;`
- `.quality-control`: `margin: 1.5rem 0 1rem; display: flex; flex-direction: column; gap: 0.5rem;`
- `input[type="range"]`: `width: 100%; cursor: pointer;`
- `#convert-btn`: `width: 100%; padding: 0.75rem; background: #0066ff; color: #fff; border: none; border-radius: 8px; font-size: 1rem; cursor: pointer;`
- `#convert-btn:disabled`: `background: #ccc; cursor: not-allowed;`
- `#convert-btn:hover:not(:disabled)`: `background: #0052cc;`
- `.status`: `margin-top: 1rem; text-align: center; color: #555;`
- `.status.error`: `color: #cc0000;`
- `#results`: `margin-top: 1.5rem;`
- `.results-header`: `display: flex; justify-content: space-between; align-items: center; margin-bottom: 1rem;`
- `.results-header h2`: `margin: 0;`
- `#download-all-btn`: `padding: 0.5rem 1rem; background: #28a745; color: #fff; border: none; border-radius: 6px; cursor: pointer;`
- `#download-all-btn:hover`: `background: #218838;`
- `#results-list`: `list-style: none; padding: 0; margin: 0; display: flex; flex-direction: column; gap: 0.5rem;`
- `.result-item`: `display: flex; justify-content: space-between; align-items: center; padding: 0.75rem; background: #f8f8f8; border-radius: 6px;`
- `.result-item a`: `padding: 0.35rem 0.75rem; background: #0066ff; color: #fff; border-radius: 4px; text-decoration: none; font-size: 0.85rem;`
- `.result-item a:hover`: `background: #0052cc;`
  </action>
  <verify>
    <automated>grep -q '<style>' /home/vs/src/heic_convert/index.html && grep -q '\.drop-zone' /home/vs/src/heic_convert/index.html && grep -q '\.dragover' /home/vs/src/heic_convert/index.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>index.html contains a complete style block with drop zone, quality control, convert button, and results list styling including the .dragover state class</done>
</task>

</tasks>

<verification>
Open index.html in a browser (file:// URL). The page should show: a centered card with title, subtitle, a dashed drop zone with "Choose files" button, a quality slider defaulting to 85, a greyed-out Convert button, and no results section visible. No console errors.
</verification>

<success_criteria>
- index.html renders a complete, styled page with all required DOM elements and correct IDs
- Both CDN script tags (heic2any, JSZip) are present
- app.js is referenced with defer
- The .dragover CSS class exists (will be toggled by JS)
- All element IDs match the contract expected by Plan 02: drop-zone, file-input, quality-slider, quality-value, convert-btn, status, results, results-list, download-all-btn
</success_criteria>

<output>
After completion, create `.planning/phases/01-working-app/01-01-SUMMARY.md`
</output>
